<%= form_for(@admin_note) do |f| %>
  <% if @admin_note.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_note.errors.count, "error") %> prohibited this admin_note from being saved:</h2>

      <ul>
      <% @admin_note.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :resource_type %><br>
    <%= f.text_field :resource_type %>
  </div>
  <div class="field">
    <%= f.label :resource_id %><br>
    <%= f.number_field :resource_id %>
  </div>
  <div class="field">
    <%= f.label :note %><br>
    <%= f.text_area :note %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
